package ${context.packageName}.${param.mapperPackageName};

import com.beastmybatis.core.mapper.CrudMapper;
import ${context.packageName}.${param.entityPackageName}.${context.javaBeanName}${context.entitySuffix};

/**
 * @author ${param.author}
 */
public interface ${context.javaBeanName}${param.mapperClassSuffix} extends CrudMapper<${context.javaBeanName}${context.entitySuffix}, ${pk.javaTypeBox}> {
}